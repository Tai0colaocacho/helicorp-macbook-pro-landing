import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "MacBook Pro Landing Page | HELICORP Round 2",
  description:
    "A premium MacBook Pro landing page redesign built with Next.js, TypeScript and Tailwind CSS.",
  openGraph: {
    title: "MacBook Pro Landing Page",
    description:
      "A premium landing page redesign for MacBook Pro with responsive UI, product features and technical specifications.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="vi">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}